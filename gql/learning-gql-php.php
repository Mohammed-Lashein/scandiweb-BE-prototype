<?php

use GraphQL\Type\Definition\ObjectType;
use GraphQL\Type\Definition\Type;

class CurrencyType extends ObjectType {
  /*
  type Currency is an Object type, so when we wrote the schema with the
  code first approach , we map type Currency to an obj type  
   */
}